/* ------------------------------- */

$(document).on('submit', '.form-action', function() {
    $.blockUI({css: {border: 'none', overflow: 'hidden !important', padding: '15px', backgroundColor: '#000', '-webkit-border-radius': '10px', '-moz-border-radius': '10px', opacity: .5, color: '#fff'}});
    var formulario = $(this);
    var metodoEnvio = formulario.attr('method');
    $.ajax({
        url: formulario.attr('action'),
        type: metodoEnvio,
        data: formulario.serialize(),
        data: new FormData(this),
        contentType: false,
        cache: false,
        processData: false,
        success: function(response) {
            $.unblockUI({});
            formulario.find('.response').html(response);
        }, 
        error: function(){
            $.unblockUI({});
            alert('An internal error has occurred.');
        }
    });
    return false;
});

/* ------------------------------- */

$(document).on('click', '.removeInfo', function(e) {
    e.preventDefault();
    vinculo = $(this).attr("data-url");
    respuesta = $(this).attr("data-response");
    alertify.confirm('<h4 class="text-bold">Are you sure you want to continue?</h4><p>Remember that the removal is permanent.</p>')
        .set('title', '<span class="text-warning"><i class="fa fa-exclamation-triangle"></i> Warning</span>')
        .set('labels', {ok: 'Continue', cancel: 'Cancel'})
        .set('onok', function() {
            $.blockUI({css: {border: 'none', overflow: 'hidden !important', padding: '15px', backgroundColor: '#000', '-webkit-border-radius': '10px', '-moz-border-radius': '10px', opacity: .5, color: '#fff'}});
            $.post(getUrl+vinculo, 
            { }, 
            function (response) {
                alertify.closeAll();
                $.unblockUI({});
                $('.'+respuesta).html(response);
            });
            return false;
        })
        .set('oncancel', function(){
            alertify.error("The process was canceled.");
        });
});

/*--------------------------*/

$(document).on('click', '.removeInfoAll', function(e) {
    e.preventDefault();
    vinculo = $(this).attr("data-url");
    respuesta = $(this).attr("data-response");
    alertify.confirm('<h4 class="text-bold">Are you sure you want to continue?</h4><p>Remember that the removal is permanent.</p>')
        .set('title', '<span class="text-warning"><i class="fa fa-exclamation-triangle"></i> Warning</span>')
        .set('labels', {ok: 'Continue', cancel: 'Cancel'})
        .set('onok', function() {
            $.blockUI({css: {border: 'none', padding: '15px', backgroundColor: '#000', '-webkit-border-radius': '10px', '-moz-border-radius': '10px', opacity: .5, color: '#fff'}});
            $.post(getUrl+vinculo, 
            {
                checkRemover: $("input[name='checkRemover']").serializeArray()
            }, 
            function (response) {
                alertify.closeAll();
                $.unblockUI({});
                $('.'+respuesta).html(response);
            });
            return false;
        })
        .set('oncancel', function(){
            alertify.error("The process was canceled.");
        });
});

/* ------------------------------- */

$(document).on('click', '.processInfo', function(e) {
    e.preventDefault();
    vinculo = $(this).attr("data-url");
    respuesta = $(this).attr("data-response");
    alertify.confirm('<h4 class="text-bold">Are you sure you want to continue?</h4><p>Remember that you are continuing according to your consent.</p>')
        .set('title', '<span class="text-warning"><i class="fa fa-exclamation-triangle"></i> Warning</span>')
        .set('labels', {ok: 'Continue', cancel: 'Cancel'})
        .set('onok', function() {
            $.blockUI({css: {border: 'none', overflow: 'hidden !important', padding: '15px', backgroundColor: '#000', '-webkit-border-radius': '10px', '-moz-border-radius': '10px', opacity: .5, color: '#fff'}});
            $.post(getUrl+vinculo, 
            { }, 
            function (response) {
                alertify.closeAll();
                $.unblockUI({});
                $('.'+respuesta).html(response);
            });
            return false;
        })
        .set('oncancel', function(){
            alertify.error("The process was canceled.");
        });
});

/* ------------------------------- */

$(document).on('click', '.processConfirmation', function(e) {
    e.preventDefault();
    vinculo = $(this).attr("data-url");
    respuesta = $(this).attr("data-response");
    alertify.confirm('<h4 class="text-bold">¿Are you sure you want to continue?</h4>')
        .set('title', '<span class="text-warning"><i class="fa fa-exclamation-triangle"></i> Warning</span>')
        .set('labels', {ok: 'Continue', cancel: 'Cancel'})
        .set('onok', function() {
            $.blockUI({css: {border: 'none', overflow: 'hidden !important', padding: '15px', backgroundColor: '#000', '-webkit-border-radius': '10px', '-moz-border-radius': '10px', opacity: .5, color: '#fff'}});
            $.post(getUrl+vinculo, 
            { }, 
            function (response) {
                alertify.closeAll();
                $.unblockUI({});
                $('.'+respuesta).html(response);
            });
            return false;
        })
        .set('oncancel', function(){
            alertify.error("The process was canceled.");
        });
});

/* ------------------------------- */

function ordenarItems(id, posicionNueva, enlace, respuesta){
    $(document).ready(function(){
        $.blockUI({css: {border: 'none', overflow: 'hidden !important', padding: '15px', backgroundColor: '#000', '-webkit-border-radius': '10px', '-moz-border-radius': '10px', opacity: .5, color: '#fff'}});
        $.post(getUrl+enlace, 
        { id: id, posicionNueva: $('#'+posicionNueva).val() }, 
        function (response) {
            $.unblockUI({});
            $('.'+respuesta).html(response);
        });
    });
    return false;
}

/* ------------------------------- */

function procesarItems(id, enlace, respuesta){
    $(document).ready(function(){
        $.blockUI({css: {border: 'none', overflow: 'hidden !important', padding: '15px', backgroundColor: '#000', '-webkit-border-radius': '10px', '-moz-border-radius': '10px', opacity: .5, color: '#fff'}});
        $.post(getUrl+enlace, 
        { id: id}, 
        function (response) {
            $.unblockUI({});
            $('.'+respuesta).html(response);
        });
    });
    return false;
}

/* ------------------------------- */

$("#checkTodo").change(function () {
    if ($(this).is(':checked')) {
        //$("input[type=checkbox]").prop('checked', true); //todos los check
        $("#infoTable input[type=checkbox]").prop('checked', true);
    } else {
        //$("input[type=checkbox]").prop('checked', false);//todos los check
        $("#infoTable input[type=checkbox]").prop('checked', false);
    }
});

/* ------------------------------- */

function init_countTa(idtextarea, idcontador, max){
    $(document).ready(function(){
        $("#"+idtextarea).keyup(function(){
            updateContadorTa(idtextarea, idcontador, max);
        });
        $("#"+idtextarea).change(function(){
            updateContadorTa(idtextarea, idcontador, max);
        });
    });
}

function updateCountTa(idtextarea, idcontador,max){
    $(document).ready(function(){
        var contador = $("#"+idcontador);
        var ta = $("#"+idtextarea);
        contador.html("0/"+max);
        contador.html(ta.val().length+"/"+max);
        if(parseInt(ta.val().length)>max){
            ta.val(ta.val().substring(0,max-1));
            contador.html(max+"/"+max);
        }
    });
}
